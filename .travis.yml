language: cpp
branches:
    only:
    - ci
compiler:
    - g++
before_install:
    - sudo add-apt-repository ppa:george-edison55/cmake-3.x
    - sudo apt-get update
    - sudo apt-get install cmake
    - sudo apt-get upgrade
script:
    - cd cbmc/src 
    - make 
    - cd ../../
    - mkdir build
    - cd build
    - cmake ..
    - make